<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>The Amour Method – Holistic Wealth Visual</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <style>
    :root {
      --bg: #05030b;
      --card: #161126;
      --gold: #ffd166;
      --pillar: #7be3ff;
      --community: #a4ffbf;
      --partner: #ff8bb3;
      --text-main: #f9f7ff;
      --text-soft: #cbbafc;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: radial-gradient(circle at top, #241a46 0%, #05030b 55%, #020008 100%);
      color: var(--text-main);
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 16px;
    }

    .card {
      width: 100%;
      max-width: 980px;
      background: radial-gradient(circle at top, #281f4c, #0a0717 70%);
      border-radius: 24px;
      padding: 20px 24px 24px;
      box-shadow: 0 24px 60px rgba(0, 0, 0, 0.7);
    }

    .header {
      display: flex;
      justify-content: space-between;
      align-items: baseline;
      gap: 12px;
      flex-wrap: wrap;
      margin-bottom: 12px;
    }

    .header h1 {
      margin: 0;
      font-size: 1.4rem;
      letter-spacing: 0.08em;
      text-transform: uppercase;
    }

    .header p {
      margin: 0;
      font-size: 0.86rem;
      color: var(--text-soft);
      max-width: 360px;
    }

    #amour-visual {
      width: 100%;
      height: 480px;
      border-radius: 18px;
      background: radial-gradient(circle at center, #1c1636, #04030b 70%);
      overflow: hidden;
      position: relative;
    }

    .legend {
      margin-top: 10px;
      display: flex;
      flex-wrap: wrap;
      gap: 8px 18px;
      font-size: 0.82rem;
      color: var(--text-soft);
    }

    .legend-item {
      display: flex;
      align-items: center;
      gap: 6px;
    }

    .legend-dot {
      width: 10px;
      height: 10px;
      border-radius: 50%;
    }

    .dot-center { background: var(--gold); }
    .dot-pillar { background: var(--pillar); }
    .dot-community { background: var(--community); }
    .dot-partner { background: var(--partner); }

    @media (max-width: 768px) {
      .card {
        border-radius: 18px;
        padding: 16px 14px 18px;
      }

      #amour-visual {
        height: 380px;
      }

      .header {
        flex-direction: column;
        align-items: flex-start;
      }

      .header h1 {
        font-size: 1.15rem;
      }
    }
  </style>
</head>
<body>
  <div class="card">
    <div class="header">
      <h1>The Amour Method™ – Holistic Wealth Map</h1>
      <p>Canetha Amour-Porter at the center • Five pillars of wellness • Community choir • Partners and amplifiers of the “Legacy Has a Sound™” movement.</p>
    </div>

    <div id="amour-visual"></div>

    <div class="legend">
      <div class="legend-item">
        <span class="legend-dot dot-center"></span> Conductor – Canetha Amour-Porter
      </div>
      <div class="legend-item">
        <span class="legend-dot dot-pillar"></span> Five Pillars – Mental • Physical • Financial • Spiritual • Relational
      </div>
      <div class="legend-item">
        <span class="legend-dot dot-community"></span> Amour Women Community – High-achieving women, mentors, leaders
      </div>
      <div class="legend-item">
        <span class="legend-dot dot-partner"></span> Partners & Amplifiers – Influencers, allies, and next-gen
      </div>
    </div>
  </div>

  <!-- compiled JS from TypeScript -->
  <script src="./amour-visual.js"></script>
</body>
</html>
